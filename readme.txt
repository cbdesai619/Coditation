Coditation Systems!

Steps to Import and Run the project:
-- Clone the master branch of the repository at the below link or import the archive emailed as attachment into the Eclipse IDE (or any other).
-- If not configured, configure the IDE and add the JRE or JDK to support java executions.
-- "Run as >> Java Application" the App.java in the package com.demo under the src path.
 

Tools used:
eclipse IDE
GIT

Please feel free to let me know in case of any queries.
